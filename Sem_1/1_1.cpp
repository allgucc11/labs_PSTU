int main ()
{
	int a,b,c ; 
	cin >> a >> b >> c ;
	if (a >= b and a >= c) {
		count << a << endl;
	}
	else if (b>=a and b>=c) { 
		count << c << endl; 
	}
else {
		count << c << endl ;
	}
}
